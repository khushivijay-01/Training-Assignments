1) Create a new folder named "git-learning-assignment"
   -mkdir git-learning-assignment
3) Initialize Git inside this folder.
    -git init
    -ls -a
4) Check the repository status.
    -git status
